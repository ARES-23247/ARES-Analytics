# ARES-Analytics Agent Rules

> **All cross-cutting conventions have been consolidated into the master workspace guide at `../../AGENTS.md`.**
> This file exists only for repo-specific notes that don't apply to the other 3 projects.

## Repo-Specific Notes

- **`ARCHITECTURE.md`** contains the detailed 9-section architecture guide (data tiers, FrameBatcher, replay engine, SysId OLS, vision calibration, hardware topology).
- **`AUDIT.md`** tracks known security findings (OAuth secret, tenant isolation, LLM SQL injection, ReplayEngine concurrency).
- The NT4 topic → Compose variable mapping table is in the **master `AGENTS.md` §4**.
- The field→canvas transform and `-90°` heading offset rules are in the **master `AGENTS.md` §5**.
